# 网页剪藏到飞书多维表格

Last Updated: 2026-09-13

当前交付为本地开发者模式安装包，尚未提交商店。

## Store Listing

名称：网页剪藏到飞书多维表格

简介：提取网页正文、选择封面图片，按三级分类一键保存到你的飞书多维表格。

功能说明：在网页右上角打开剪藏浮层，先选封面、编辑标题和提示词，再依次选择提示词类型、分类、风格与模型，记录会写入分类对应的数据表。设置页支持新增或删除同级分类，并可将自定义分类绑定到新增的飞书子表。桌面端提供更高的浮层和更大的提示词预览区，较小窗口会自动适配；候选图片横向浏览，底部操作按钮固定。可返回编辑，同页收起再打开保留草稿。上传失败时保留内容并允许重试，成功后显示保存确认。需要用户配置自己的飞书应用和目标表格。

分类：Productivity。主要语言：简体中文。

## Graphics & Assets

1.7.4 修复 X 视频识别和站内切换，无新增界面或权限；沿用 1.7.0 视频选择界面素材即可。

1.7.3 分类设置新增「添加同级分类」表单与分类删除入口。检查截图见 output/playwright/options-141-full.png，使用模拟内容；上架前需刷新正式设置页截图。

1.4.0 采用八子表三级分类模板：设置页新增「类型与分类」「风格与模型」两个可自定义文本框；面板第二步改为「类型 → 分类 → 风格 → 模型」逐级选择。检查截图见 output/playwright/panel-step2-full.png、options-e2e.png，使用演示内容；上架前替换为商店尺寸的正式截图。

1.3.0 重新设计网页内两步浮层，浅色卡片、靛紫选中态，独立提示词编辑区与横向图片条。检查截图位于 output/playwright/panel-content-130.png、panel-organize-130.png、panel-success-130.png，使用演示内容；上架前替换为商店尺寸的正式截图。

1.2.1 更新了设置页使用说明与模板链接，下次发布前需刷新设置页展开使用说明的截图。

当前开发版更新了弹窗选中状态与同步成功反馈：分类、模型和封面使用靛蓝强调色，成功提示展示常驻确认卡片并带短暂淡入动效，支持减少动态效果偏好。下次发布前需刷新分类与封面选中、同步成功两种状态的界面截图。

1.1.1 使用新的圆角液态玻璃书签图标：工具栏提供 16、32、48、128 像素 PNG，另提供 256 像素预览图。图标外部背景透明，弹窗与设置页同步使用新图标。生成母图与提示词存于 output/design。未来上架截图需使用新图标与新界面。output/playwright 中的检查截图使用模拟内容，不是商店规格素材。

## Permissions Justification

| 权限 | 用途 |
| --- | --- |
| activeTab | 用户点击插件后读取当前网页标题、链接和内容 |
| scripting | 用户发起剪藏时执行正文和图片提取 |
| storage | 本机保存用户配置、三级分类体系与按应用区分的登录令牌缓存 |
| <all_urls> | 从用户选择的任意 HTTP(S) 网页及图片站点读取内容，并连接飞书保存剪藏；不是只为飞书接口申请 |

1.4.0 未新增权限。

## Privacy & Data Use

1.4.0 将分类从「一级标签 + 模型」扩展为「类型 → 分类 → 风格 → 模型」三级，配置保存在本机浏览器；保存时按所选分类写入用户自有的对应数据表。未新增权限或数据接收方。

1.3.0 的未保存草稿仅保留在当前网页的插件浮层中，刷新、跳转后不保留。只在用户点击插件时注入；应用密钥与令牌不传给网页浮层。没有新增权限或数据接收方。

应用凭据、分类和令牌缓存保存在本机浏览器中。获取令牌时应用凭据发送给飞书；用户点击保存时，标题、正文、链接、所选分类及封面图片发送给用户配置的飞书表格。图片候选预览及下载会访问原图片站点；页面下载兜底可能携带该站点的登录 Cookie。没有新增分析、遥测或发布者服务器。

公开隐私政策地址、发布者名称、联系邮箱、支持地址与分发范围尚未提供，未来提交商店前需补齐。

## Version History

### 1.7.4 · 2026-09-13

- X 播放器尚未生成 video 标签时，可从原帖封面和新版嵌套媒体数据恢复 MP4 清晰度选项，并优先最高画质。
- 修复 X 站内切换后仍使用旧帖子地址的问题；第一次点击插件即重新读取当前帖子。
- 同一帖子的 `/video/1` 等地址变体不会被误判为页面变化；未新增权限。

### 1.7.3 · 2026-09-13

- 设置页支持在任一提示词类型下新增或删除同级分类，并绑定新增的飞书子表。
- 选择已同步子表时可按表名自动填写分类名称，并读取主分类、风格和模型；未新增权限。

### 1.7.2 · 2026-09-13

- 桌面端剪藏浮层最大高度提升至 900px，并将新增空间优先分配给提示词编辑区。
- 较矮或较窄的窗口继续按可用视口收敛，底部操作区保持可见；未新增权限。

### 1.4.1 · 2026-09-09

- 修复即梦详情页重复提取整页推荐正文：优先读取唯一明确提示词区域，短提示词有效，推荐、导航、空区域和 SPA 切换隔离。
- 新增视频提示词-成品提示词及五种默认风格；保留用户自定义分类兼容。
- 设置页改为可视化分组卡片和标签编辑，并提供只读飞书子表 schema 同步。
- 校验飞书字段类型和选项；单选写入字符串，多选写入数组。


- 适配八子表提示词模板：生图（人物/环境/生物/海报）与视频（镜头画面/运镜方案/影视效应/人物情绪）各四张数据表。
- 面板第二步改为三级选择：提示词类型 → 分类（决定写入哪张数据表）→ 风格与模型；类型与分类必选，风格与模型可选。
- 保存时按分类自动定位数据表，写入「分类」「主分类」「子分类」「使用模型」字段；设置页可自定义三级分类体系。
- 拦截新模板链接，提示先创建副本；不新增权限。

### 1.3.0 · 2026-09-07

- 工具栏点击改为在网页右上角打开浮层，采用两步剪藏流程。
- 固定高度图片预览、横向缩略图与独立提示词编辑区，避免图片过多挤压正文。
- 支持草稿收起恢复、返回编辑、快捷键保存、失败重试与成功确认；取消分类时清除不适用模型。
- 保留 9 字段模板与原有图片上传流程，不新增权限。

### 1.2.1 · 2026-09-07

- 设置页采用新版 9 字段剪藏模板，说明复制、授权、剪藏流程及各字段用途。
- 保留旧模板拦截，并增加新模板拦截，提示用户创建副本后使用。
- 更新使用文档；不新增权限。

### 1.1.2 · 2026-09-06

- 修复 X / Twitter 短帖摘录被推荐流污染：按原帖时间戳链接匹配帖子 ID，只读取该帖正文。
- 保留短文本、参数、换行与表情，排除回复、引用卡片及趋势栏；支持动态正文等待，未定位到目标时留空并提示。
- 普通文章不再因为字数少而被更大容器覆盖，移除整页纯文本兜底。
- 24 项浏览器样例断言、弹窗提取结果展示检查与 12 项同步回归通过。真实 X 链接在检查环境中被拒绝访问；未新增权限或数据传输。

### 1.1.1 · 2026-09-06

- 重新设计圆角液态玻璃图标，采用通透薄荷玻璃底板与翡翠绿书签。
- 更新工具栏、弹窗和设置页中的品牌图标，增加 32 像素图标以适配工具栏显示。
- 已检查所有声明图标的实际尺寸与透明圆角；本次不涉及同步功能与权限变更。

### 1.1.0 · 2026-09-06

- 修复封面上传目标表格错误、图片失败仍显示成功的问题。
- 修复刷新候选后的选择错位，兼容链接中指定的数据表。
- 修复正文转换库在浏览器加载时的导出错误，避免初始化中断。
- 重做扁平极简弹窗和设置页，完善选中提示、键盘操作、错误重试及防重复点击。
- 完成 12 项模拟同步回归检查及浏览器交互检查；真实飞书端到端写入待用户环境验证。
